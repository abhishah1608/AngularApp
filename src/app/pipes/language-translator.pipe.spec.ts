import { LanguageTranslatorPipe } from './language-translator.pipe';

describe('LanguageTranslatorPipe', () => {
  it('create an instance', () => {
    const pipe = new LanguageTranslatorPipe();
    expect(pipe).toBeTruthy();
  });
});
